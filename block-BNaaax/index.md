In sample directory, create a file server.js and inside it run following commands using node server.js

console.log(__dirname);
console.log(__filename);
use path module to join __dirname and server.js
